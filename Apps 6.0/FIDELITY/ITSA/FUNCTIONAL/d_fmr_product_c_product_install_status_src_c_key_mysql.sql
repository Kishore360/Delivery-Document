SELECT CASE WHEN count(1)  THEN 'FAILURE' ELSE 'SUCCESS' END as Result, CASE WHEN count(1)  THEN 'MDS to DWH data validation failed for d_fmr_product_c.product_install_status_src_c_key' ELSE 'SUCCESS' END as Message FROM <<tenant>>_mdsdb.u_fmr_product_final  SRC JOIN <<tenant>>_mdwdb.d_fmr_product_c TRGT ON (SRC.sys_id = TRGT.row_id AND SRC.sourceinstance = TRGT.source_id )  LEFT JOIN <<tenant>>_mdwdb.d_lov LKP ON ( concat( 'INSTALL_STATUS~FMR_PRODUCT_C~~~' ,upper( SRC.install_status)) = LKP.row_id AND SRC.sourceinstance = LKP.source_id ) WHERE COALESCE(LKP.row_key,CASE WHEN SRC.install_status IS NULL THEN 0 else -1 end)<> (TRGT.product_install_status_src_c_key) 
