SELECT CASE WHEN count(1)  THEN 'FAILURE' ELSE 'SUCCESS' END as Result, CASE WHEN count(1)  THEN 'MDS to DWH data validation failed for d_jira_teamid_c.u_team_id_c' ELSE 'SUCCESS' END as Message FROM discover_mdsdb.u_jira_teamid_final  SRC JOIN discover_mdwdb.d_jira_teamid_c TRGT ON (SRC.sys_id = TRGT.row_id  AND SRC.sourceinstance = TRGT.source_id )  WHERE (COALESCE(SRC.u_team_id,'UNSPECIFIED')) <> (TRGT.u_team_id_c) and SRC.cdctype='X'
