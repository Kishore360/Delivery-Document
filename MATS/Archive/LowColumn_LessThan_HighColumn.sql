


SELECT CASE WHEN COUNT(*)> 0 THEN 'FAILURE' ELSE 'SUCCESS' END AS Result,CASE WHEN COUNT(*)> 0 THEN '#LOWER_COLUMN > #HIGHER_COLUMN for' || COUNT(*) || ' records in #TABLE_NAME' ELSE '#LOWER_COLUMN > #HIGHER_COLUMN for no records in #TABLE_NAME ' END AS Message FROM #TABLE_NAME WHERE #LOWER_COLUMN > #HIGHER_COLUMN
